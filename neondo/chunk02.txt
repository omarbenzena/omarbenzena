.type||'SHIFT').toUpperCase())}</span><b>${pay(j)}</b></div><h3>${esc(j.title)}</h3><p>${esc(j.events?.title||'Berlin opportunity')} · ${esc(j.events?.location||'Berlin')}</p><p class="date">${datefmt(j.start_at)}</p><div class="tags">${(j.tags||[j.role]).slice(0,3).map(t=>`<span>${esc(t)}</span>`).join('')}</div><button class="job-action" onclick="applyShift('${j.id}')"><span>View & ${T.find==='Find work'?'apply':'apply'}</span><span>↗</span></button></div></article>`).join('')}
function landing(){let filtered=state.filter==='All'?state.jobs:state.jobs.filter(j=>(j.role+' '+j.title+' '+(j.tags||[]).join(' ')).toLowerCase().includes(state.filter.toLowerCase()));return `${header()}<main><section class="hero"><div class="hero-left"><div class="hero-copy"><div class="eyebrow"><span class="live-dot"></span> BERLIN · CREATIVE + EVENT WORK</div><h1>${heroTitle()}</h1><p>${esc(T.sub)}</p><div class="hero-actions"><button class="solid-btn" onclick="document.querySelector('#opportunities').scrollIntoView()"><span>${T.explore}</span><span>↗</span></button><button class="text-link" onclick="openAuth('signup','worker')">${T.profile}</button></div><div class="popular-roles"><a href="#opportunities" onclick="setFilter('DJ')">DJ</a><a href="#opportunities" onclick="setFilter('Photographer')">Photographer</a><a href="#opportunities" onclick="setFilter('Videographer')">Videographer</a><a href="#opportunities" onclick="setFilter('Stagehand')">Stagehand</a><a href="#opportunities" onclick="setFilter('Runner')">Runner</a><a href="#roles">AV</a><a href="#roles">Lighting</a><a href="#roles">Host / Hostess</a><a href="#roles">Service</a></div><div class="proof-row"><div class="faces"><span>OM</span><span>MK</span><span>JL</span></div><span><b>Real work. Real pay.</b><br>No follower count required.</span></div></div></div><div class="hero-art"><div class="checker"></div><div class="sticker">NO<br>EXPOSURE<br>GIGS</div><div class="phone"><div class="phone-top"><span>Tonight in Berlin</span><b>•••</b></div><div class="poster"><span class="feature-tag">FEATURED</span><span class="poster-ring"></span><strong>AFTER<br>HOURS</strong><small>CREATIVE NIGHT · BERLIN</small></div><div class="phone-job"><span class="role-icon">◎</span><div><small>CONTENT</small><b>Event photographer</b><p>Tonight · 22:00</p></div><strong>€180</strong></div><button class="phone-cta" onclick="setFilter('Photographer');document.querySelector('#opportunities').scrollIntoView()">View opportunity →</button></div></div></section><section class="opps" id="opportunities"><div class="container"><div class="section-head"><div><span class="kicker">${T.live}</span><h2>${T.paid}</h2></div><p>Every listing shows pay, hours, location and expectations upfront.</p></div><div class="filters">${[['All','For you'],['DJ','DJs'],['Photographer','Photography'],['Videographer','Video + content'],['Stagehand','Stage crew'],['Production','Production']].map(([v,l])=>`<button class="filter ${state.filter===v?'active':''}" onclick="setFilter('${v}')">${l}</button>`).join('')}</div><div class="job-grid">${cards(filtered)}</div><div class="all-opps"><button onclick="setFilter('All')">See every live opportunity ↗</button></div></div></section><section class="roles" id="roles"><div class="container"><div class="roles-head"><span class="kicker">${T.whole}</span><h2>${T.ways}</h2><p>From the person behind the decks to the crew behind the stage, bar, camera and control desk — NEONDO is built for the whole event.</p></div><div class="role-grid">${ROLE_CATALOG.map(([n,d,q],i)=>`<a class="role-card" href="#opportunities" onclick="setFilter('${q}')"><small>${String(i+1).padStart(2,'0')}</small><div><strong>${n}</strong><p>${d}</p></div><b>↗</b></a>`).join('')}</div></div></section><section class="how" id="how"><div class="container how-grid"><div><span class="kicker">HOW NEONDO WORKS</span><h2>${lang==='en'?`Your creative career,<br><em>built gig by gig.</em>`:esc(T.creative)}</h2><p class="how-lead">You don’t need the right connections. Show what you can do, show up, and turn completed work into proof.</p><button class="how-cta" onclick="openAuth('signup','worker')">Build your NEONDO profile →</button></div><div class="steps"><div class="step"><span>01</span><h3>FIND</h3><p>Browse clear paid opportunities matched to your role, skills and availability.</p></div><div class="step"><span>02</span><h3>DO</h3><p>Get selected, receive the brief and know exactly where you need to be.</p></div><div class="step"><span>03</span><h3>PROVE</h3><p>Completed gigs, approved hours, skills and ratings build your professional record.</p></div><div class="step"><span>04</span><h3>GROW</h3><p>Get invited back, become a favourite and unlock stronger opportunities.</p></div></div></div></section><section class="profile-section"><div class="container profile-grid"><div class="profile-copy"><span class="kicker">THE NEONDO PROFILE</span><h2>${T.proof}</h2><p>No empty stars. No public popularity contest. Your profile grows from real completed work, verified skills and reliability.</p><ul><li>✓ Verified completed work</li><li>✓ Portfolio, equipment and skills</li><li>✓ Certificates and availability</li><li>✓ Ratings from real bookings</li></ul><button class="profile-outline" onclick="openAuth('signup','worker')">Create your profile ↗</button></div><div class="profile-card"><div class="profile-top"><span class="avatar">MK</span><div><h3>Maya K.</h3><p>Photographer · Content creator</p><small>Berlin · EN / DE</small></div><b class="availability">AVAILABLE</b></div><div class="stats"><div><strong>12</strong><small>GIGS</small></div><div><strong>100%</strong><small>SHOW-UP</small></div><div><strong>4.9</strong><small>RATING</small></div></div><div class="skillline"><b>VERIFIED SKILLS</b><div class="skill-tags"><span>Event photography</span><span>Vertical video</span><span>Lightroom</span></div></div><div class="recent"><b>RECENT WORK</b><p><b>Nightlife photographer</b><span>Lumen Sessions · Completed</span></p><p><b>Brand event content</b><span>Studio North · Completed</span></p></div></div></div></section><section class="organizer"><span class="kicker">FOR ORGANIZERS + COMPANIES</span><h2>${lang==='en'?`Post once.<br>Find the right people.`:esc(T.org)}</h2><p>Build the whole event team in one place — DJs, photographers, creators, performers, stage crew, technicians, hosts, hospitality and logistics.</p><button class="cta-btn" onclick="startPost()">Post your first opportunity →</button><small>Clear rates · verified profiles · rebook your favourites</small></section><footer class="footer"><div class="container footer-grid"><div>${brand()}<p>Creative + event work, built for Berlin.</p></div><div class="footer-links"><a href="#opportunities">Opportunities</a><a href="#roles">Roles</a><a href="#how">How it works</a><a href="#" onclick="openLegal('Impressum');return false">Impressum</a><a href="#" onclick="openLegal('Datenschutz');return false">Datenschutz</a><a href="#" onclick="openLegal('AGB');return false">AGB</a></div><small>Berlin pilot · 18+ only</small></div></footer></main>`}
function setFilter(v){state.filter=v;render();setTimeout(()=>document.querySelector('#opportunities')?.scrollIntoView({behavior:'smooth'}),20)}
function openAuth(mode='login',role='worker'){document.body.insertAdjacentHTML('beforeend',`<div class="modal-back" id="auth"><div class="modal"><div class="modal-head"><h2>${mode==='login'?'Welcome back':'Join NEONDO'}</h2><button class="close" onclick="$('#auth').remove()">✕</button></div><div class="auth-tabs"><button class="${mode==='login'?'active':''}" onclick="$('#auth').remove();openAuth('login')">Log in</button><button class="${mode==='signup'?'active':''}" onclick="$('#auth').remove();openAuth('signup','worker')">Sign up</button></div>${mode==='signup'?`<div class="field"><label>I want to</label><select id="arole"><option value="worker" ${role==='worker'?'selected':''}>Find creative/event work</option><option value="company" ${role==='company'?'selected':''}>Hire crew + creatives</option></select></div><div class="form-row"><div class="field"><label>First name</label><input id="afirst"></div><div class="field"><label>Last name</label><input id="alast"></div></div><div class="field"><label>Company name (companies)</label><input id="acompany"></div>`:''}<div class="field"><label>Email</label><input id="aemail" type="email"></div><div class="field"><label>Password</label><input id="apass" type="password" minlength="8"></div><button class="solid-btn" style="width:100%" onclick="${mode==='login'?'doLogin()':'doSignup()'}"><span>${mode==='login'?'Log in':'Create account'}</span><span>↗</span></button><div id="authmsg"></div></div></div>`)}
async function doLogin(){if(!sb)return toast('Authentication service is loading. Try again in a moment.','warn');$('#authmsg').innerHTML='<div class="notice">Signing in…</div>';let {data,error}=await sb.auth.signInWithPassword({email:$('#aemail').value.trim(),password:$('#apass').value});if(error)return $('#authmsg').innerHTML=`<div class="notice err">${esc(error.message)}</div>`;state.user=data.user;await loadProfile();$('#auth').remove();state.screen='dashboard';state.route='overview';render()}
async function doSignup(){if(!sb)return toast('Authentication service is loading. Try again in a moment.','warn');let role=$('#arole').value,email=$('#aemail').value.trim(),password=$('#apass').value;if(password.length<8)return $('#authmsg').innerHTML='<div class="notice err">Use at least 8 password characters.</div>';let {data,error}=await sb.auth.signUp({email,password,options:{data:{role,first_name:$('#afirst').value.trim(),last_name:$('#alast').value.trim(),company_name:$('#acompany').value.trim()},emailRedirectTo:location.origin}});if(error)return $('#authmsg').innerHTML=`<div class="notice err">${esc(error.message)}</div>`;if(data.session){state.user=data.user;await new Promise(r=>setTimeout(r,450));await loadProfile();$('#auth').remove();state.screen='dashboard';state.route='profile';render()}else $('#authmsg').innerHTML='<div class="notice ok">Account created. Check your email to confirm it, then log in.</div>'}
async function saveShift(id){if(id.startsWith('demo-'))return toast('Demo opportunity saved.');if(!state.profile||state.profile.role!=='worker')return openAuth('login','worker');let {error}=await sb.from('saved_shifts').upsert({worker_id:state.user.id,shift_id:id});toast(error?error.message:'Saved.',error?'err':'ok')}
async function applyShift(id){if(id.startsWith('demo-')){if(!state.profile)return openAuth('signup','worker');return toast('Demo application sent.')}if(!state.profile)return openAuth('signup','worker');if(state.profile.role!=='worker')return toast('Use a worker account to apply.','warn');if(!state.profile.onboarding_complete)return toast('Complete your profile first.','warn');let {error}=await sb.from('applications').insert({shift_id:id,worker_id:state.user.id});toast(error?error.message:'Application sent.',error?'err':'ok')}
function startPost(){if(!state.profile)return openAuth('signup','company');if(state.profile.role!=='company')return toast('Use a company account to post opportunities.','warn');state.screen='dashboard';state.route='create';render()}
function demoDashboard(role='worker'){state.demoRole=role;state.profile={id:'demo',role,first_name:'Omar',company_name:'NEONDO Demo',verified:true,onboarding_complete:true};state.worker=role==='worker'?{primary_role:'Stagehand',city:'Berlin',rating_avg:4.9}:null;state.company=role==='company'?{company_name:'NEONDO Demo Events'}:null;state.screen='dashboard';state.route='overview';render()}
function navItems(){let r=state.profile?.role||'worker';if(r==='worker')return [['overview','⌂','Overview'],['find','⌕','Find work'],['schedule','▣','Schedule'],['availability','◷','Availability'],['earnings','€','Earnings'],['messages','✉','Messages'],['profile','◎','Profile'],['notifications','●','Notifications'],['account','⚙','Account']];if(r==='company')return [['overview','⌂','Overview'],['events','▣','Events'],['create','＋','Post opportunity'],['crew','◎','Find talent'],['applications','✓','Applicants'],['timesheets','◷','Timesheets'],['messages','✉','Messages'],['profile','⚙','Company'],['account','⚙','Account']];return [['overview','⌂','Admin'],['users','◎','Users'],['events','▣','Events'],['account','⚙','Account']]}
function dashboardShell(){let n=navItems();return `<div class="dashboard"><div class="dash-top"><div class="container dash-top-inner"><a href="#" onclick="goLanding();return false">${brand()}</a><div class="dash-actions">${langSelect()}<button class="icon-btn" onclick="setTheme()">◐</button><button class="small-btn" onclick="logout()">Log out</button></div></div></div><div class="dash-shell"><aside class="sidebar">${n.map(x=>`<button class="${state.route===x[0]?'active':''}" onclick="go('${x[0]}')">${x[1]} &nbsp; ${x[2]}</button>`).join('')}</aside><main class="dash-content" id="dashPage"></main></div><nav class="bottom-nav">${n.slice(0,4).map(x=>`<button onclick="go('${x[0]}')"><b>${x[1]}</b>${x[2]}</button>`).join('')}<button onclick="openMore()"><b>•••</b>More</button></nav></div>`}
function go(r){state.route=r;render();renderDash()}
function goLanding(){state.screen='landing';state.profile=state.user?state.profile:null;render()}
function openMore(){document.body.insertAdjacentHTML('beforeend',`<div class="modal-back" id="more"><div class="modal"><div class="modal-head"><h2>Menu</h2><button class="close" onclick="$('#more').remove()">✕</button></div>${navItems().map(x=>`<button class="outline-btn" style="width:100%;display:block;text-align:left;margin:5px 0" onclick="$('#more').remove();go('${x[0]}')">${x[1]} &nbsp; ${x[2]}</button>`).join('')}</div></div>`)}
async function renderDash(){let el=$('#dashPage');if(!el||!state.profile)return;let r=state.profile.role;try{if(state.route==='overview')el.innerHTML=await dashOverview(r);else if(state.route==='find')el.innerHTML=`<h1 class="dash-title">Find work</h1><p class="dash-sub">Creative projects, performances and event shifts around Berlin.</p><div class="dash-job-grid">${cards(state.jobs)}</div>`;else if(state.route==='schedule')el.innerHTML=await schedulePage();else if(state.route==='availability')el.innerHTML=await availabilityPage();else if(state.route==='earnings')el.innerHTML=await earningsPage();else if(state.route==='messages')el.innerHTML=await messagesPage();else if(state.route==='profile')el.innerHTML=await profilePage(r);else if(state.route==='notifications')el.innerHTML=await notificationsPage();else if(state.route==='events')el.innerHTML=await eventsPage();else if(state.route==='create')el.innerHTML=createPage();else if(state.route==='crew')el.innerHTML=await crewPage();else if(state.route==='applications')el.innerHTML=await applicationsPage();else if(state.route==='timesheets')el.innerHTML=await companyTimesheets();else if(state.route==='users')el.innerHTML=await usersPage();else if(state.route==='account')el.innerHTML=accountPage();}catch(e){el.innerHTML=`<div class="notice err">${esc(e.message||e)}</div>`}}
async function dashOverview(r){if(state.demoRole)return r==='worker'?`<h1 class="dash-title">Hey, Omar 👋</h1><p class="dash-sub">Your creative + event work in one place.</p><div class="metrics"><div class="metric"><b>2</b><span>UPCOMING GIGS</span></div><div class="metric"><b>1</b><span>APPLICATIONS</span></div><div class="metric"><b>4.9</b><span>RATING</span></div><div class="metric"><b>€684</b><span>THIS MONTH</span></div></div><div class="panel"><div class="panel-head"><h3>Next booking</h3></div><div class="item"><div><h4>Nightlife photographer</h4><p>Fri 28 Aug · 22:00 · Neukölln</p></div><span class="status">CONFIRMED</span></div></div>`:`<h1 class="dash-title">NEONDO Demo Events</h1><p class="dash-sub">Find and rebook the right people.</p><div class="metrics"><div class="metric"><b>3</b><span>UPCOMING EVENTS</span></div><div class="metric"><b>18</b><span>PEOPLE BOOKED</span></div><div class="metric"><b>4</b><span>OPEN POSITIONS</span></div><div class="metric"><b>126</b><span>APPROVED HOURS</span></div></div>`;if(r==='worker'){let [{data:a},{data:b},{data:t}]=await Promise.all([sb.from('applications').select('id,status').eq('worker_id',state.user.id),sb.from('bookings').select('id,status,shifts(title,start_at,events(location))').eq('worker_id',state.user.id),sb.from('timesheets').select('id,status').eq('worker_id',state.user.id)]);return `<h1 class="dash-title">Hey, ${esc(state.profile.first_name||'crew')} 👋</h1><p class="dash-sub">Your next work, applications and verified history.</p>${!state.profile.onboarding_complete?'<div class="notice warn">Finish your profile before applying to live opportunities. <button class="small-btn" onclick="go(\'profile\')">Complete profile</button></div>':''}<div class="metrics"><div class="metric"><b>${(b||[]).filter(x=>x.status==='confirmed').length}</b><span>UPCOMING</span></div><div class="metric"><b>${(a||[]).filter(x=>x.status==='applied').length}</b><span>APPLICATIONS</span></div><div class="metric"><b>${state.worker?.rating_avg||'—'}</b><span>RATING</span></div><div class="metric"><b>${(t||[]).filter(x=>x.status==='approved').length}</b><span>APPROVED GIGS</span></div></div><div class="panel"><div class="panel-head"><h3>Next bookings</h3><button class="small-btn" onclick="go('find')">Find work</button></div>${(b||[]).filter(x=>x.status==='confirmed').slice(0,4).map(x=>`<div class="item"><div><h4>${esc(x.shifts?.title||'Booking')}</h4><p>${datefmt(x.shifts?.start_at)} · ${esc(x.shifts?.events?.location||'Berlin')}</p></div><span class="status">CONFIRMED</span></div>`).join('')||'<div class="empty">No confirmed bookings yet.</div>'}</div>`}if(r==='company'){let [{data:e},{data:b}]=await Promise.all([sb.from('events').select('*').eq('company_id',state.user.id),sb.from('bookings').select('*').eq('company_id',state.user.id)]);return `<h1 class="dash-title">${esc(state.company?.company_name||state.profile.company_name||'Company')